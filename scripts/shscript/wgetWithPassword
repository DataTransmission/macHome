#/bin/bash
echo '"This script is used when wget requires authentication with username and password"'
echo ''
echo 'Enter Username'
read username
echo 'Enter Website Directory https://...'
read website
wget --user=$username --ask-password $website
